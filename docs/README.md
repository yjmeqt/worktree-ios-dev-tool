# Historical design docs

These documents were written when the tool was called `pulse-ios-dev-tool` and lived inside the `pulse-dev-skills` monorepo. Paths, names, and package references reflect that era — the code has since been renamed to `worktree-ios-dev-tool` and extracted to its own repo.

They're kept for reference; read them for architectural intent, not for current file paths or CLI names.
